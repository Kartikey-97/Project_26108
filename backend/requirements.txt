# Core web framework
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9      # required for FastAPI file uploads

# Settings / config
pydantic>=2.7.0
pydantic-settings>=2.3.0

# Database (PostgreSQL + async driver)
sqlalchemy[asyncio]>=2.0.0
asyncpg>=0.29.0
alembic>=1.13.0

# pgvector — vector similarity search inside PostgreSQL
pgvector>=0.3.0

# Document processing
pdfplumber>=0.11.0           # PDF text extraction (better than PyPDF2 for structured docs)
python-docx>=1.1.0           # DOCX extraction

# HTTP client — for source adapters calling external APIs/portals
httpx>=0.27.0

# Utilities
python-dotenv>=1.0.0
